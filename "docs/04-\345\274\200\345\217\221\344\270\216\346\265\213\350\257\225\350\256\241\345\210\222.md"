# 04 · 项目开发与测试计划

**文档版本**：V1.2
**编制日期**：2026-09-15
**关联文档**：01-项目设计文档 V1.3（架构/契约/指标）/ 02-设计思路 V1.2（ADR 1~12）/ 03-算法选择 V1.2（选型）
**计划范围**：M1 ~ M5 全部里程碑的任务分解、依赖关系、验收标准与测试体系

> **V1.2 修订记录**：① M5-T05 并入 M2-T06 遗留的"1 万组滚动 ≥ 50fps"帧率定量验证与 512MB+ 分段路径运行时验证（按 08 §7.5 审查结论，补位此前被误挂到 M4-T05 而未执行的性能门禁）；② 附录 A 补"构建链版本一致性"门禁（D17 根治后的回归防线）。
> **V1.1 修订记录**：① M1 任务同步"FileKey 按需解析"（阶段 1.5）与缓存生命周期；② M4-T06 改为后端单一事实源（弃 localStorage 双写）；③ 安全测试补 S8（ENOENT 语义）；④ 新增附录 A 发布 checklist 展开；⑤ CI 表对齐 01 §8 的 .github/workflows；⑥ M2 增加设置页与失败清单任务。

---

## 1. 计划总览

### 1.1 里程碑总览

| 里程碑 | 主题 | 核心交付 | 验收锚点 |
|---|---|---|---|
| **M1** | 算法内核 | internal/ 引擎全量 + CLI 冒烟 + benchgen | 基准集 C 达标（01 §5.3） |
| **M2** | 前端框架 | API 契约实现 + 扫描/进度/结果 UI + 设置页 | 全流程可视（手动删） |
| **M3** | 操作闭环 | 三平台回收站 + 保留策略 + 校验删除 | 三平台实测 + L5 门禁通过 |
| **M4** | 缓存与体验 | SQLite 缓存 + 预览 + 快捷键 | 二次扫描提速 ≥ 10x |
| **M5** | 打磨发布 | 报告导出 + i18n + 三平台安装包 | 附录 A 发布 checklist 全过 |

依赖主线：`M1 → M2 → M3 → M4 → M5`。允许的并行：M2 前端骨架（T01~T03）可与 M1 后半段并行（契约先行，前端 mock 开发）；M4-T01 缓存可与 M3 并行。

### 1.2 任务编号规则

`M{里程碑}-T{序号}`；每任务含 **DoD（Definition of Done，完成定义）**，DoD 未满足不得关闭任务。

---

## 2. 开发计划详述

### 2.1 M1 — 算法内核（引擎层，不依赖 Wails）

| 任务 | 内容 | 模块 | 依赖 | DoD |
|---|---|---|---|---|
| M1-T01 | Go 模块初始化、internal 包骨架、.github/workflows CI（gofmt/vet/test） | 根 | — | `go build ./...` 通过；CI 模板生效 |
| M1-T02 | 数据模型：01 §7 全部结构（含 OpRequest/OpsResult/KeepPolicy/FailedItem 等契约类型） | model | T01 | 与 01 §7 一一对应；可序列化 |
| M1-T03 | 基础目录遍历（符号链接跳过、0 字节跳过、过滤器接入；unix 顺带 FileKey） | scanner | T02 | 遍历结果与 `find` 对照一致 |
| M1-T04 | FileKey 平台抽象：darwin/linux（lstat 附带）/ **windows（按需解析接口 + `GetFileInformationByHandle`）** | scanner | T03 | 三平台 build tag 编译通过；本平台单测过；win 路径含"延迟解析"用例 |
| M1-T05 | 并行遍历：目录队列 + worker；重叠根目录去重（大小写按平台折叠） | scanner | T03,T04 | `-race` 通过；重叠根不重复收集 |
| M1-T06 | 过滤器：扩展名/大小/路径 glob/隐藏文件（MinSize 默认 0=不限） | filter | T02 | 每规则独立单测 + 组合用例 |
| M1-T07 | 首尾预筛哈希：一次 open 两次 ReadAt；**小文件 ≤128KiB 一趟双哈希**；win 同句柄顺带 FileKey 解析 | hasher | T04 | 小/大文件哈希正确性单测；句柄即用即关 |
| M1-T08 | 全量 BLAKE3（lukechampine.com/blake3）；大文件 >512MB 按 16MiB 切片并行 + tree hash 合并 | hasher | T07 | 分段结果 == 顺序哈希结果（强等价测试） |
| M1-T09 | 流水线编排 + 状态机 + **阶段 1.5 候选组 FileKey 去重** + 暂停/恢复/取消语义（01 §5.5） | dedup | T05~T08 | 状态转换单测全覆盖；硬链接去重用例；取消后无 goroutine 泄漏 |
| M1-T10 | 进度统计：原子计数 + 500ms 节流回调（引擎层回调接口，不绑 Wails） | progress | T09 | 计数准确；节流单测 |
| M1-T11 | CLI 冒烟工具（cmd/fdd-cli：扫描指定目录输出 JSON 报告） | cmd | T09,T10 | 真实目录跑通；JSON 可解析 |
| M1-T12 | benchgen：数据集 A/B/C 生成器（01 §12.2，随机种子可复现） | cmd | — | 三数据集生成 + 校验脚本 |
| M1-T13 | 基准首测：数据集 C 实测耗时/内存/吞吐，回填 03 §5.2 | 全 | T11,T12 | 实测报告入库 docs/；未达标则回根因分析 |

### 2.2 M2 — 前端框架（可与 M1 后半段部分并行）

| 任务 | 内容 | 依赖 | DoD |
|---|---|---|---|
| M2-T01 | Wails v2 初始化（vue-ts 模板）、暗/亮主题 CSS 变量、布局骨架（侧边导航+三页签） | M1-T01 | `wails dev` 可运行；主题切换即时生效 |
| M2-T02 | API 契约实现：app.go 落地 01 §3.2 全部 15 方法（含 GetFailedItems/GetVersion）；TS 绑定生成 | M1-T09 | 全部方法可从前端调用；TS 类型生成一致 |
| M2-T03 | 事件流接入：scan:progress/stage/done/cancelled/error 订阅与 Pinia 状态落库 | M2-T02 | 进度条实时刷新无卡顿（500ms 节流生效） |
| M2-T04 | 扫描页：目录拖拽/系统选择器/路径粘贴；过滤器表单（渐进披露高级项） | M2-T02 | 三种添加方式可用；过滤器序列化正确 |
| M2-T05 | 扫描进行页：阶段进度、实时统计、暂停/继续/取消；完成后自动切结果页 | M2-T03 | 三按钮语义正确；ETA 未知时显示不定进度 |
| M2-T06 | 结果列表页：DuplicateGroup 虚拟滚动卡片、组内文件行、保留建议高亮 | M2-T03 | 1 万组滚动 ≥ 50fps；分页查询正确 |
| M2-T07 | 结果排序/筛选：按可释放空间/扩展名/仅显示勾选 | M2-T06 | 各排序筛选组合查询正确 |
| M2-T08 | 设置页（后端 Settings 单一事实源读写）+ 失败清单入口（GetFailedItems 展示） | M2-T02 | 设置重启保留；失败项可查看导出 |

### 2.3 M3 — 操作闭环（安全核心）

| 任务 | 内容 | 依赖 | DoD |
|---|---|---|---|
| M3-T01 | 保留策略引擎：newest/oldest/shortest/directory/manual；默认建议（路径最短且非隐藏） | M2-T06 | 每策略单测；默认建议可解释 |
| M3-T02 | 操作前校验：逐文件重算 BLAKE3 比对；**ENOENT → Skipped** | M1-T08 | 篡改 100% 拦截；消失文件计 Skipped 不计失败 |
| M3-T03 | 回收站三平台：darwin（NSWorkspace）/windows（SHFileOperationW）/linux（自研 XDG Trash） | — | 三平台实测进回收站可还原 |
| M3-T04 | 移动至目录（跨盘复制+校验+删源）与永久删除（显式选择+二次确认） | M3-T02 | 跨盘移动后内容校验通过 |
| M3-T05 | 硬链接合并（可选，同卷才可用，失败降级提示） | M1-T04 | 同卷合并后 FileKey 一致 |
| M3-T06 | 执行反馈：ops:progress/done 事件、失败/跳过清单展示、回收站打开引导 | M2-T03 | 失败与跳过分别可见可导出 |
| M3-T07 | RevealInFolder 三平台（open -R / explorer /select / xdg-open） | — | 三平台定位选中正确 |

### 2.4 M4 — 缓存与体验

| 任务 | 内容 | 依赖 | DoD |
|---|---|---|---|
| M4-T01 | SQLite 缓存：schema+PRAGMA（01 §7.3）、命中判定、**任务结束批量写回**、last_hit 淘汰、路径消失清理、CacheStats/Clear | M1-T13 | 二次扫描提速 ≥ 10x（数据集 C 实测） |
| M4-T02 | 增量扫描：仅 mtime/size 变化文件重算；断点续扫（记录已扫目录） | M4-T01 | 修改 1 文件后重扫只处理该文件 |
| M4-T03 | 跨任务暂停/恢复（挂起状态持久化，配合缓存） | M1-T09,M4-T01 | 进程重启后可恢复 |
| M4-T04 | 预览面板：图片缩略图（限尺寸）/文本前 4KB/HEX 前 256B | M2-T06 | 三类预览正确渲染 |
| M4-T05 | 快捷键：Space 预览 / Cmd(Ctrl)+A 全选组 / Del 标记 / Esc | M2-T06 | 快捷键清单实测 |
| M4-T06 | 设置持久化完善：**后端 settings.json 单一事实源**（弃 localStorage 双写），GetSettings/SaveSettings 闭环 | M2-T08 | 双端无双源漂移；重启设置保留 |

### 2.5 M5 — 打磨发布

| 任务 | 内容 | 依赖 | DoD |
|---|---|---|---|
| M5-T01 | 导出报告 HTML/CSV/JSON（路径转义、失败与跳过清单附后） | M2-T07 | 三格式可被 Excel/浏览器正确打开 |
| M5-T02 | 错误处理统一：权限拒绝/IO 失败隔离展示，不阻塞主流程 | 全 | 失败文件不中断扫描（数据集注入测试） |
| M5-T03 | i18n：vue-i18n 中/英字典全覆盖 | M2-T01 | 切换无遗漏文案 |
| M5-T04 | 三平台打包：图标、Windows manifest（longPathAware）、安装包 | 全 | 三平台安装冒烟通过 |
| M5-T05 | 性能终测 + 基准报告（对照 01 §5.3 全指标）；**并补做 M2-T06 遗留的"1 万组滚动 ≥ 50fps"帧率定量验证**（含 512MB+ 分段路径运行时验证，数据集 B 已可由 `benchgen -dataset B` 生成） | M4 | 报告入库 docs/；50fps 门禁给出定量结论并回填 06 报告 |
| M5-T06 | 发布流程：版本号、许可（MIT）、release notes、checksum | M5-T04 | 附录 A checklist 全过 |

---

## 3. 测试计划

### 3.1 测试分层总览（与 01 §12.1 对齐）

| 层级 | 目标 | 工具/方法 | 执行时机 |
|---|---|---|---|
| L1 单元 | 模块逻辑正确 | `go test` / `vitest` | 每次提交 |
| L2 并发 | 数据竞争与泄漏 | `go test -race` + goroutine 泄漏检测 | 每次提交 |
| L3 集成/E2E | 全流程正确 | 合成数据集脚本 + 真实目录冒烟 | 每日 / 里程碑 |
| L4 平台适配 | 三平台差异 | build tag 集成测试 + 手测 checklist | M3 起每里程碑 |
| L5 安全 | 防误删 | 定向注入测试（S1~S8） | M3 起，发布前必过 |
| L6 性能基准 | 指标与回归 | benchgen + `go test -bench` + pprof | M1 首测，此后每 PR |

### 3.2 单元测试用例清单（L1）

| 模块 | 关键用例 |
|---|---|
| scanner | ① 遍历结果与 find 对照；② 符号链接/0 字节跳过；③ 重叠根目录（`a/` + `a/b/`）只收集一次；④ mac/win 大小写折叠比较；⑤ win FileKey 延迟解析（未解析时 Resolved=false，不参与去重比较） |
| filter | ① 单规则各维度（ext/min/max/exclude glob/hidden）；② 组合规则与优先级；③ 边界：空列表=全通过、MaxSize=0=不限、MinSize 默认 0 |
| hasher | ① 首尾 128KiB 指纹正确性；② **小文件 ≤128KiB：一趟双哈希，xxHash+BLAKE3 均为全文件哈希**；③ 恰好 128KiB / 128KiB+1 边界；④ 大文件分段并行结果 == 顺序哈希结果；⑤ ReadAt 失败注入（进失败清单不 panic） |
| dedup | ① 构造已知重复集 → 分组数量/成员/可释放空间全对；② **阶段 1.5：硬链接（同 FileKey）只计一次，组内剩 1 个则整组淘汰**；③ 状态机全部合法转换 + 非法转换拒绝；④ 暂停时 in-flight 完成后挂起；⑤ 取消后无泄漏 |
| cache | ① 命中（path/size/mtime 全一致）；② mtime 变化 → 未命中重算；③ 批量写回事务性（中断不留半写）；④ last_hit 淘汰与路径消失清理；⑤ 库损坏 → 容错重建不崩溃 |
| ops | ① 校验拦截（文件被改）；② **ENOENT → Skipped（不计失败不计空间）**；③ 移动冲突命名 name_1.ext；④ XDG trashinfo 格式合规；⑤ 保留项不可进入删除清单 |
| progress | 原子计数准确；500ms 节流不丢终值 |

### 3.3 集成与端到端（L3）

1. **确定性 E2E**：benchgen 固定种子生成已知重复结构 → `fdd-cli` 扫描 → 断言：重复组数、每组文件、总可释放空间与生成器记录**完全一致**；
2. **契约测试**：前端 TS interface 与 wails 生成绑定 diff 为空（防契约漂移）；
3. **硬链接 E2E**：数据集中注入硬链接组 → 断言不被计为重复、不占可释放空间；
4. **真实目录冒烟**：对本项目 docs/ 与一个真实素材目录跑全流程，人工核对。

### 3.4 并发与稳定性（L2）

- 全量 `-race`；
- 取消/暂停时序矩阵：各阶段 × 三操作，验证无泄漏、无死锁；
- 数据集 A（10 万小文件）长跑：RSS 曲线平稳（无缓冲池泄漏）。

### 3.5 平台适配 checklist（L4）

| # | 检查项 | macOS | Windows | Linux |
|---|---|---|---|---|
| 1 | FileKey 正确（复制/硬链接两组用例） | ☐ | ☐ | ☐ |
| 2 | Windows FileKey 按需解析不影响非候选文件性能（抽样句柄计数） | n/a | ☐ | n/a |
| 3 | 删除进回收站且可还原 | ☐ | ☐ | ☐ |
| 4 | 长路径（>260 字符）扫描与删除 | n/a | ☐ | n/a |
| 5 | 大小写折叠根目录去重 | ☐ | ☐ | n/a |
| 6 | RevealInFolder 选中定位 | ☐ | ☐ | ☐ |
| 7 | 中文/特殊字符文件名（空格、emoji、换行） | ☐ | ☐ | ☐ |
| 8 | 配置/缓存目录落在平台规范位置（01 §5.4） | ☐ | ☐ | ☐ |
| 9 | 安装包安装/卸载 | ☐ | ☐ | ☐ |

### 3.6 安全测试（L5，发布门禁）

| # | 场景 | 预期 |
|---|---|---|
| S1 | 扫描完成后、删除执行前篡改文件 | 校验拦截，计失败清单 |
| S2 | 保留策略选中的文件 | 任何批量操作不可选中删除 |
| S3 | 硬链接副本 | 不计入可释放空间；不作为删除目标 |
| S4 | 永久删除 | 必须显式选择 + 二次确认 |
| S5 | 回收站失败（磁盘满/权限） | 明确报错，不静默丢文件 |
| S6 | 0 字节文件 | 默认跳过 |
| S7 | 操作进行中拔盘/断网盘 | 失败清单呈现，其余继续 |
| S8 | 操作时文件已被外部删除（ENOENT） | 计入 Skipped，不算失败、不计释放空间 |

### 3.7 性能基准（L6）

- 数据集 A/B/C 定义见 01 §12.2（benchgen 固定种子可复现）；
- 指标：总耗时、哈希吞吐 GB/s、峰值 RSS、CPU 利用率；
- 方法：`go test -bench` + 运行时 `runtime/pprof`（CPU/heap）定位热点；
- **回归红线**：数据集 C 总耗时劣化 > 10% 的 PR 需附说明方可合入；
- 达标线：对照 01 §5.3（首扫 ≤ 30s / 二扫 ≤ 5s / RSS ≤ 300MB / 吞吐 ≥ 1.5GB/s）。

### 3.8 UI 验收（L3 人工脚本）

核心旅程：拖入目录 → 配置过滤器 → 扫描（观察进度/暂停/恢复）→ 应用"保留最短路径" → 预览抽查 3 组 → 删除到回收站 → 系统回收站验证还原。附加：主题切换、i18n 切换、万组列表滚动流畅度、失败清单入口。

### 3.9 缺陷管理

| 级别 | 定义 | 处理 |
|---|---|---|
| P0 | 误删/数据损坏/崩溃 | 立即修复，阻塞发布 |
| P1 | 主流程功能失效 | 当前里程碑内修复 |
| P2 | 体验问题（卡顿/文案） | 排期修复 |
| P3 | 瑕疵 | backlog |

---

## 4. CI/CD 流水线（.github/workflows/）

| 触发 | 阶段 | 内容 |
|---|---|---|
| PR | 检查 | gofmt + `go vet` + `go test -race ./...` + eslint + 前端 build + 契约 diff 检查 |
| PR（可选） | 基准 | 数据集 A 快速基准，超红线评论标记 |
| nightly | 全量 | 数据集 C 完整基准 + pprof 报告归档 |
| main | 构建 | 三平台产物（wails build）+ 冒烟 |
| tag | 发布 | 安装包 + SHA-256 checksum + release notes（模板：变更/已知问题/基准数据） |

> **落地状态（M4 交付后）**：`.github/workflows/ci.yml` 已创建，覆盖 PR 检查阶段的 **gofmt + go vet + go test -race + 前端 build** 四项，四平台交叉编译沿用本地验证（M1-T01 已过）。**未纳入**：eslint / vitest（前端尚未配置该工具链）、契约 diff 检查（需专门的"Go json tag ↔ TS 接口"校验器，见 08 §7.5 第 3~4 项）；基准/nightly/构建/发布四个阶段属 M5 范围。

---

## 5. 里程碑验收门禁汇总

| 里程碑 | 硬性门禁 |
|---|---|
| M1 | 数据集 C 达标（01 §5.3）；`-race` 全绿；E2E 确定性用例（含硬链接组）通过 |
| M2 | 核心旅程可视跑通；1 万组滚动流畅；契约 diff 为空 |
| M3 | L5 安全用例 S1~S8 全过；三平台回收站 checklist 通过 |
| M4 | 二次扫描提速 ≥ 10x 实测；重启恢复可用；缓存损坏自愈 |
| M5 | 三平台安装冒烟；性能终测报告入库；附录 A 全过 |

---

## 6. 风险与依赖

| 风险/依赖 | 影响 | 缓解 |
|---|---|---|
| Go BLAKE3 实现性能不及预期 | M1 指标风险 | M1-T13 前置实测；备选 zeebo/blake3 或退 SHA-256（指标重估） |
| Windows 测试环境 | M3/M5 平台验证 | 提前准备 VM/真机；FileKey 与回收站在 M1-T04 即出单测版 |
| Windows FileKey 按需解析引入复杂度 | M1 逻辑风险 | 阶段 1.5 顺序单测固化；unix/win 行为差异用集成用例双覆盖 |
| Wails v2 平台 bug | M5 打包 | 引擎与框架解耦（internal/ 不 import wails）；问题可绕行 |
| 基准环境噪声（SSD 差异） | 回归误报 | 固定测试机；基准取 3 次中位数 |
| 功能蔓延（相似图片等） | 交付延期 | 严格 V1.0 范围（01 §1.2）；V2 需求入 backlog |

---

## 7. 交付物清单

| 类别 | 交付物 |
|---|---|
| 代码 | internal/ 引擎、frontend/ UI、cmd/（fdd-cli、benchgen）、.github/workflows、build/ 打包资源 |
| 测试 | L1~L6 测试与记录、平台 checklist、基准报告（docs/） |
| 文档 | 01/02/03/04 四份文档 + 发布 checklist + 用户使用说明（随 M5） |
| 产物 | 三平台安装包 + SHA-256 checksum + release notes |

---

## 附录 A：发布 checklist（M5-T06 展开）

**代码与质量**
- [ ] 全部里程碑门禁通过（§5）
- [ ] `go test -race` / vitest 全绿；L5 安全用例 S1~S8 复测通过
- [ ] 基准终测数据回填 03 §5.2 并入库 docs/

**打包**
- [ ] 三平台安装包构建 + 安装/卸载冒烟（L4 #9）
- [ ] Windows manifest 含 longPathAware；图标正确
- [ ] 二进制体积 ≤ 20MB（01 §5.3）
- [ ] 依赖许可清单核对（全 MIT/BSD，无 GPL）
- [ ] **构建链版本一致性**：`wails version` 与 go.mod 中 wails 库版本一致；`wails build` 前后 go.mod/go.sum 校验和不变（防 CLI 静默改写依赖，D17 根治后的回归门禁）

**发布物**
- [ ] 版本号（语义化 vX.Y.Z）与 GetVersion 一致
- [ ] LICENSE（MIT）与第三方声明文件
- [ ] SHA-256 checksum 文件
- [ ] release notes：变更列表 / 已知问题 / 基准数据 / 升级说明
- [ ] 用户使用说明（中文）

**发布后**
- [ ] 官方渠道发布与公告
- [ ] P0/P1 缺陷值班响应（发布后两周内）
