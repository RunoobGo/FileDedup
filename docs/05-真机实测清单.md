# 05 · 真机实测清单（M7 平台证据包 · 三腿）

> 定位：本文是 **M7（平台证据包）** 的执行清单（04 §6.11 登记表尾注登记的"下一批开工项"，原话"下一批开工只能是 M7（平台证据包）"）。
> 汇总自 `docs/04` §3.5 / §6.5 / §6.6.3 / §6.6.4 / §6.9.8 / §6.11 与后端代码逐文件盘点（域 A~R）。
> 每项给出：来源编号 → 操作方式 → 结果验证（判据）→ 需记录的读数。
> 状态口径：只记 **PASS / FAIL / SKIP(原因)**，不许把"CI 某条腿绿"读成"真机验证过"（见 §0.1）。
>
> **三腿分段**（2026-09-24 M209 扩写，设计段 `docs/superpowers/specs/2026-09-24-checklist-three-leg-design.md`）：
> 下文 **P0~P10 = W 腿（Windows）**，行号前缀 `W*-*`；**`## MAC` = darwin 腿**、**`## LNX` = linux 腿**。
> 本文原名 `05-Windows真机实测清单.md`，扩腿后改名；W 段的既有条目一字未改（它们本来就是 Windows 格）。

---

## 0. 总则

### 0.1 三条基础事实（决定本清单为何必须存在）

1. **CI 有 windows 真机腿，但日志分不清"真跑"与"整包 Skip"**：`scripts/test-windows-quarantine.sh` 在 `QUARANTINE` 空清单分支里的 `exec go test -count=1 ./...` 与 CI 三条腿都不带 `-v`（04 以短语"三条腿都不带 `-v`"多处自认，登记行为 §6.11 表内 **M100**）。因此凡标注"CI windows 腿已绿"的项目，其 Skip 出口项一律按"无读数"处理。
2. **判定逻辑住在无 tag 文件里，Linux 主门禁只证明判据表，不证明 Windows 胶水真读到过正确的字节**（ads / sysguard / realbytes / cloudfile / fscase / recycle_policy 同族分层纪律）。
3. **CI 三条腿都不起 GUI，也不碰真实回收站**：现读 `.github/workflows/ci.yml`，三腿的 step 只有 `go vet` / `go test -race` / `npm` 构建 / `fdd-cli` 冒烟（跨卷冒烟刻意只在 ubuntu 跑），同一文件头注"尚未纳入"里自认两条——"macOS/Linux 真实回收站与 GUI 端到端：需人工或桌面会话""macOS 侧跨卷冒烟：harness 依赖 losetup/mount -o loop，仅 Linux 可用"。⇒ **窗口前置/合并、真实回收站两类格在三腿上都是零读数**，darwin 与 linux 在这一点上跟 windows 处境相同；这就是本文从单腿扩成三腿的全部理由（04 §6.36 五的 ★ 格、P10 末尾的 ★ 注）。
   ★ **"真两卷"不在这条零读数清单里，但它只兑现了一半**（〔2026-09-24 M209 当日现读更正，本条初稿写的是"三腿都不给真两卷"，那是过强口径〕）：ubuntu 腿的 `scripts/smoke-symlink.sh` 真挂 loop + ext4（当日 CI 日志原文 `卷 A dev=2049 ｜ 卷 B dev=1792`，且"① 硬链接跨卷（预期失败）"拿到的是内核报的**真实** `Invalid cross-device link`），七条判据当轮全成立——所以"独立卷"这一形态在 CI 上**有**读数。它没覆盖的是 **LNX-2 要的那一段**：回收站复制腿（`copy + .trashinfo + 删源`）。那一腿在 Go 用例里靠 `forceCrossVolumeRename` **注入** EXDEV（`move_crossvolume_identity_test.go`），CI 从没在真卷上跑过。⇒ LNX-2 照旧要现场环境，只是**不必再另找第二块物理盘**：同一台 ubuntu 机器上把 `XDG_DATA_HOME` 指到那个 loop 卷即可复现（本格已按此改写）。

### 0.2 上真机之前可先做的三件事（不占真机时间）

| # | 事项 | 落点 |
|---|---|---|
| A1 | windows 腿补一次带 `-v` 的跑法，把 `--- SKIP` 计数纳入读数 | `test-windows-quarantine.sh` 的 `QUARANTINE` 空清单分支（`exec go test -count=1 ./...`，M100 遗留动作）→ 直接产出 V8b、`fsid_path`、`trash_windows_recycle` 等 12 条"绿但不知是否真跑"用例的差分 |
| A2 | 修 `TestUndoHardlinkSelfHealsAfterUnlinkCrash`（`internal/ops/undo_i6_test.go`）里的**假前提 Skip**（Skip 文案"Windows 上 inode 身份未解析，走的是大小降级分支"，已被 fsid I7 推翻）→ 让 undoHardlink 强校验路径在 windows 腿重新暴露 | 域 E |
| A3 | 前端 pathpolicy 混例（盘符 / UNC / 尾分隔符 / 相对路径）是 node 用例，不必真机 | `plans/2026-09-22-full-review-revision.md` Task C2「高危判据测试缺口补齐（R3-2）」Step 1 `isGroupCrossVolume 用例`（〔2026-09-24 M205 勘误〕此处原文引用的编号"AS-H6·D-1"在计划文件里零命中，引用本身错） |

### 0.3 环境要求（分腿）

#### 0.3-W · Windows（P0~P10 用）

- **主机**：Win10 1607+ 或 Win11（longPathAware / 开发者模式 / UAC 都依赖版本）；建议 Win11。
- **权限**：同一台机器上既能以**管理员**跑一次，也能以**普通账户（开发者模式开/关两态）**跑（§6.5 E 组四环境）。
- **卷**：NTFS 系统盘 + 至少一个第二物理卷（跨卷腿）+ 一个 **exFAT 或 FAT32 U 盘**（保留名夹具、`index==0` 卷、`CreateHardLinkW` 拒绝路径都靠它）+ 可选 SMB/网络卷、ReFS、BitLocker 卷。
- **注册表可写**：`HKCU\...\Explorer\BitBucket`（NukeOnDelete 场景）、`HKLM\...\FileSystem\LongPathsEnabled`（长路径两态）。
- **云客户端**：一个已配置 OneDrive 账户（占位文件场景 M31）。
- **杀软现实性**：至少一轮在**带第三方 AV / Defender 实时防护开启**的机器上跑（04 §6.6.3 拒绝原因表点名的"北信源防病毒接管/清理 `$Recycle.Bin`"、§6.6.4 点名的"目标环境（本机 Win11 + 北信源）`LongPathsEnabled` 取值未确认"两类干扰面：句柄占用、Minifilter 卷重定向、回收站目录接管）。
- **产物**：用 `wails build -platform windows/amd64` 或 build.yml 产出的 zip 裸 exe（未签名）；**不要**用 `GOOS=windows go test` 交叉跑代替 GUI 验收。
- **读数留档**：每项记录 OS 版本 / 卷文件系统 / 是否提权 / 开发者模式态 / AV 状态，按对应 M 编号回填 04。

#### 0.3-M · macOS（`## MAC` 用，2026-09-24 M209 新增）

- **主机**：Apple Silicon 或 Intel 的**真实 macOS 桌面**，且执行者处在**GUI 登录会话**里（SSH 进去的会话没有窗口服务器，MAC-1/MAC-2 直接不成立——那不是"未通过"，是"环境不满足"，按 SKIP 记）。
- **版本留档**：macOS 版本 + 系统卷/数据卷是否分离（`/System/Volumes/Data` 形态影响 MAC-3 与 LNX-1 那类跨卷对照），以及是否 FileVault。
- **iCloud**：MAC-4 需要**已配置 iCloud Drive 且能生成占位文件**的账户，并且**用户在场同意下载配额**——这条前提是 M31 登记时就写死的，不是清单自己新加的（原文见 04 的 M31 行"可兑现形状"）。
- **终端可见**：MAC-2 的**唯一**痕迹打在标准输出上，所以那一格必须从终端启动裸可执行文件（`FileDedup.app/Contents/MacOS/FileDedup`），从 Finder 双击启动会看不到。
- **产物**：`wails build -platform darwin/universal` 的 `.app`；不要用 `GOOS=darwin go test` 代替 GUI 验收（同 0.3-W 那条纪律）。

#### 0.3-L · Linux（`## LNX` 用，2026-09-24 M209 新增）

- **主机**：一台真实 Linux 桌面/会话。LNX-5 需要**两态各跑一次**：有 D-Bus 会话总线、没有会话总线。
- **卷**：至少两个**不同文件系统**的挂载点（第二块盘或 `losetup -f --show` + `mkfs` 的 loop 卷即可），LNX-2/LNX-4 靠它。★ 现读：CI 的 ubuntu 腿就是这一形态（`smoke-symlink.sh` 真挂 loop + ext4 并拿到真 EXDEV，见 §0.1 第 3 条那条更正）——所以 LNX-2 的"两卷"半边可以就地复用，缺的只是把 `XDG_DATA_HOME` 指过去那一步。
- **网络文件系统**：LNX-4 需要挂得上 sshfs 或 NFS 其中之一；**挂不上就按 SKIP + 原因记**，不得用本地卷的读数替代（该格测的就是"该卷不跟踪块数"这一环境形态）。
- **GNU coreutils**：LNX-6 需要 `timeout` 命令在位（busybox 版不算——判据点是 rc 与 `--kill-after` 语义）。
- **桌面环境**：LNX-2 的"回收站看得见"要一个遵循 freedesktop Trash 规范的 DE（GNOME Files / Dolphin 任一）。
- **产物**：`wails build -platform linux/amd64`，或直接 `fdd-cli`（无 GUI 的那几格 CLI 就够，LNX-5 必须有 GUI）。


---

> **以下 P0~P10 全部是 W（Windows）腿的格子。** darwin 腿见 `## MAC`，linux 腿见 `## LNX`；两节末尾的"跨腿共格提示"给出哪些 W 格在另两腿上需要同一条判据的读数。

## P0 · 回收站防线（H6 / §6.6.3 E 组）——最高优先级

失败模式是**静默永久删除**，且三层防线（预检 fail-open → 事后复核可 Skip → executor 严格回退只有注入测试）**没有任何一层有真机读数**（域 A/T-5）。

| # | 场景 | 操作方式 | 结果验证（判据） |
|---|---|---|---|
| W1-1 | 正常入站 | 对 NTFS 卷上一批小文件（含中文名、含 >1000 项混合列表）执行"移入回收站" | ① 源路径消失；② 文件出现在**该卷** `$Recycle.Bin`（系统回收站可见、可"还原"回原位）；③ 应用历史里 kind=trash、`DestPath` 恒空（结构性缺失，记录读数即可）、徽标/title 走 `undoableFor(trash,windows)`＝不可应用内回撤；④ 三判据事后复核（源不在 + 该卷条目增量==预期 + 入站时间基线）全部通过，无「可能已被直接删除，请立即到回收站核实」误报（`executor.go` trash 回退分支文案，见 W1-3 勘误） |
| W1-2 | 超配额拒绝 | 把测试卷回收站属性容量设为 **1MB**，对单个 >1MB 文件执行移入回收站 | 见**含容量数字的拒绝提示**（`capacityReason`），文件仍在原位；不得出现"报成功但文件消失" |
| W1-3 | 批量部分超限 | 混合"能进 / 不能进"的多文件一批执行 | **整批拒绝**（`internal/ops/trash_windows.go` 的 `defaultTrash` 预检注释原话"任一不满足即**整批拒绝**"，判据函数 `recyclableReason`/`recycleBinCapacityReason`）；若走 executor 逐个回退，被 Shell 静默丢弃的条目必须记 Failed（`Execute` 的 trash 回退分支，文案「可能已被直接删除，请立即到回收站核实」——〔2026-09-24 M205 勘误〕此清单旧版把该文案写作"疑似已被直接删除"，与码内字面有别，grep 时以 `executor.go` 现文为准），而非 Skipped。此语义 Windows 独有、至今零真机观测 |
| W1-4 | NukeOnDelete 全局 | `reg add HKCU\Software\Microsoft\Windows\CurrentVersion\Explorer\BitBucket /v NukeOnDelete /t REG_DWORD /d 1` 后执行 | 预检**拒绝并指名策略**（nukeStatus=on）；删除键后恢复放行 |
| W1-5 | NukeOnDelete 按卷 | 在 `BitBucket\Volume\{GUID}` 下按卷设 1（GUID 来自 `GetVolumeNameForVolumeMountPointW`，顺带证 `volumeGUID` 解析） | **按卷覆盖全局**的 MSDN 优先级成立：该卷拒绝、他卷放行 |
| W1-6 | HKLM 机器级策略 | 若条件允许，在组策略禁用回收站的机器上执行 | 当前代码**只读 HKCU**（域 B）→ 预期行为是"预检放行、事后复核兜住"；记录兜住与否的真实读数（这是已登记缺口，测出什么都算收账） |
| W1-7 | SHQueryRecycleBinW / 卷 GUID | 让带 `-v` 的 windows 腿（或本机 `go test ./internal/ops -run Recycle -v`，需 GTest 环境）真跑 `trash_windows_recycle_test.go` | 四组可 Skip 用例（`TestVolumeGUIDShape` / `TestRecycleBinCapBytesSystemDrive` / `TestQueryRecycleBinSystemDrive` / Nuke 三态两条：`TestWinNukeStatusOfDistinguishesAbsentFromUnreadable` + `TestRecycleBinVolumeNukeSystemDrive`）**逐条报 PASS 而非 SKIP**；`shQueryRBInfoSize==20` 布局钉在真 Shell 上语义正确 |
| W1-8 | 长路径回收站 | >260 深度文件执行移入回收站 | `SHFileOperationW` 不接受 `\\?\`（`defaultTrash` 内注释原话"SHFileOperation 不支持 \\?\ 前缀，使用普通路径"）且不在 longPathAware 解除清单（04 §6.6.4"回收站路径（跨盘移入回收站）在超长路径上仍可能失败"段）→ 预期**显式失败并保留源文件**，不得静默永久删除 |
| W1-9 | Shell 静默降级 | 沙箱/被安全软件接管 `$Recycle.Bin` 的环境（或第三方清理工具在场）执行 | Shell 返回 0 但未进站时，事后复核必须报「可能已被直接删除，请立即到回收站核实」——这条防线自身是本清单的被测对象 |
| W1-10 | 并发腿的计数串扰（**M197 兑现格**） | 构造"一条腿会被 Shell 静默永久删除、另一条腿正常入站"的并发：把测试卷回收站容量压到刚好让 A 组超限（照 W1-2/W1-3 的配比），同批混入 B 组小文件，让执行器 C6 回退腿并发跑（`executor.go` 的 `runIndexed(…, opWorkers, …)`） | ① A 组逐条记 **Failed** 且源仍在原位（或被如实报出），**不得**因为 B 组推进了该卷条目数就被判成功——这就是 M197 的判据本体；② 观察调用是否真排队：`defaultTrash` 现经 `withTrashSerial`（`internal/ops/trash_serial.go`）串行，Process Monitor 里两条 `SHFileOperationW` 的时间窗**不应重叠**；③ 回退腿吞吐因串行而下降的幅度记录进账（本裁定的自觉代价）。★ 本机 darwin 只能证 `withTrashSerial` 的排队语义（`trash_serial_test.go` 真跑，摘锁即红），"windows 真接了这条列 + 判据不再被旁路"两格**只有这里有读数** |

---

## P1 · 长路径链路（T04 / §3.5#4 / §6.6.4 / M33）

manifest 的 `ws2:longPathAware` 元素已声明（`build/windows/wails.exe.manifest`）但**注册表前提、双条件生效、进程缓存**全部未复测。

| # | 场景 | 操作方式 | 结果验证 |
|---|---|---|---|
| W2-1 | 长路径扫描/移动/回撤 | 构造 >260 字符深度的重复文件若干，`LongPathsEnabled=1` 态下扫描→移入回收站→（非 trash 类）回撤 | 扫描可见、操作不报 `ERROR_PATH_NOT_FOUND`；回撤成功 |
| W2-2 | 注册表未开启对照 | `LongPathsEnabled=0` + 重启后重复 W2-1 | 退化为 MAX_PATH 行为；确认失败被**报成显式 Failed** 而非静默丢条目 |
| W2-3 | ADS 守卫长路径 fail-open（**M33 开放项**） | 在接近/超过 260 深度造一条带命名流的文件：`type nul > "长路径文件.txt:note"`，对其执行去重动作 | 判据应仍 **Reject**（守卫不因 `FindFirstStreamW` 报 2/3 落进 ErrMissing→Allow）。若放行＝M33 从"推断"升级为"真机读数"，回到代码侧补 `\\?\` 重试 |
| W2-4 | 实占口径退化面 | 深目录下观察 realbytes 列 | `GetCompressedFileSizeW` 失败时 `ActualKnown=false`，界面须显示"未知"而非 0（域 H） |

---

## P2 · 软链接 / 重解析点（§6.5 E 组四环境 + junction）

`CreateSymbolicLinkW` 两段式与错误码翻译（1314/87/50/5）至今只过 `go vet`，`test-windows-quarantine.sh` 头注原话"在 Linux 上**只经过 go vet 的交叉编译检查，未经真实调用**"（开放项 E）。

| # | 场景 | 操作方式 | 结果验证 |
|---|---|---|---|
| W3-1 | 提权 + 跨卷 | 管理员运行应用，对两个 NTFS 卷的重复内容执行软链接合并 | 成功；`VolumeResolved` 为真（`app.go` 的 `FileView.VolumeResolved` 字段，`toGroupView` 赋值）、前端显示跨卷入口（`frontend/src/utils/pathpolicy.ts` 的 `isGroupCrossVolume`） |
| W3-2 | 非提权 + 开发者模式**关** | 普通账户、开发者模式关闭 | **整体失败 + 恰一条**环境级权限指引（AggregateWarnings 折叠为一条，条目数不变），且**无任何文件被改动**；错误码文案指向"提权或开开发者模式"而非"环境不支持"（两段式降级顺序：先 0x2 标志、遇 87 再去标志重试——顺序反了会把环境不支持报成权限不足） |
| W3-3 | 非提权 + 开发者模式**开** | 开开发者模式后重复 | 成功（0x2 标志路径走通） |
| W3-4 | 同卷 | 同卷组执行 | 成功 + 提示建议改用硬链接 |
| W3-5 | 悬空链接 | 合并成功后拔掉目标盘/删除目标 | 记录页标红「链接已失效」（`SymlinkStatus`）；**回撤仍应成功**；若失效未标红 → §6.5 C 组补的 ACL/杀软致 Lstat 失败嫌疑点成立 |
| W3-6 | verifySymlinked 强校验档位 | 合并后手动把链接指向第三个文件再回撤 | Windows 上身份解析成功时应走强校验（`fsid.FromPath`）判否；若恒走"只比大小"弱校验＝I7 叙述在真机不成立 |
| W3-7 | junction / 卷挂载点回环 | 扫描含 `C:\Users\All Users`、手工 `mklink /J` 目录挂载点、`SUBST` 盘的根 | 扫描**不回环**、不死循环（代码零覆盖，见域 F 末行）；Go `Lstat` 对 mount-point 重解析点的 `Mode` 归属当场读数并回填注释 |

---

## P3 · 跨卷移动 / 回撤 / 错误码（域 C）

`isCrossDeviceExtra` 认 error 17 的分支**没有一条用例真跨卷 rename 过**；跨卷腿的旧欠账"本机 uid=501 挂不了独立卷"已被 04 §6.17 七更新口径——Linux/loop 卷侧已有 CI 读数，**Windows 侧仍无真机读数**（该脚本不在 windows job 里）。

| # | 场景 | 操作方式 | 结果验证 |
|---|---|---|---|
| W4-1 | 跨卷 move 一次 | 两物理卷间对 ~200 个文件执行移动（`TestAggregateWarningsCollapsesPrivilegeFailures` 头注点名的欠账） | 走"复制+校验+identityStill+删源"腿；字节/条目记账正确（M40 假账族核账）；不出现"源没删/目标缺" |
| W4-2 | 跨卷回撤 | W4-1 之后执行回撤 | `restoreInPlace` 跨卷分支成功；目标卷同名被占时报错而非覆盖（Windows `os.Rename`＝MOVEFILE_REPLACE_EXISTING 静默替换风险，M19 抢占在 Windows 的残余 TOCTOU 记录读数） |
| W4-3 | 杀软持句柄 | 对正被预览/索引/同步占用的文件执行移动与暂存清理 | `ERROR_SHARING_VIOLATION(32)` 路径（`workTempRemove`）：重试/残留/报错三者实际行为记录，暂存目录不得静默堆积 |
| W4-4 | 盘符≠卷 | `SUBST X: D:\somefolder` 后跨该"卷"操作；同一目录先后挂两个不同卷 | 记录盘符键分裂/混池的实际症状（`TestFileVolumePrefersResolvedIdentity` 注释原话"Windows 上一个盘符未必是一个卷（挂载点会把别的卷挂到 C:\Mount\X）"，`app_volume_test.go`）；回收站判据 2 是否按 GUID 而非盘符正确归卷 |
| W4-5 | explorer 假告警 | 逐条点"打开所在文件夹"（Reveal，#6），路径含空格、逗号、中文各一 | `explorer.exe /select,` 常**成功也返回非零** → 观察 M58 的 `onExit` 报警是否每次误弹（域 N，高概率缺陷）；`OpenTrash`（`shell:RecycleBinFolder`）打开正确 SID 的回收站 |

---

## P4 · 物理身份 / 硬链接 / 卷型（域 D/E/H/J）

| # | 场景 | 操作方式 | 结果验证 |
|---|---|---|---|
| W5-1 | fsid 逐卷读数 | 在 NTFS / exFAT / FAT32 / SMB / ReFS / BitLocker（可得者）上各解析一次文件身份 | NTFS：`Resolved=true` 且 FileId 稳定；FAT/exFAT：应落 `index==0→未解析` 分支；逐卷记"解析成功/失败"原始读数（域 T-3，不许只记用例绿） |
| W5-2 | 硬链接合并端到端（**L4 #1 欠账**） | 同 NTFS 卷上对重复文件执行硬链接合并→回撤 | `CreateHardLinkW` 成功且 `verifyHardlinked` 的 `os.SameFile` 判真；回撤恢复两文件。**带 AV 的机器上特别注意 Minifilter 骗过 SameFile 的可能**（域 E），必要时用 `fsutil hardlink list` 外部核对 |
| W5-3 | 硬链接在 exFAT | 对 exFAT U 盘执行合并 | `CreateHardLinkW` 应返回 50 并被翻译成可读失败，不是 panic/静默 |
| W5-4 | 写锁定文件身份 | 一进程独占写打开文件时执行 `identityStill` 五处复核 | `TestIdentitySurvivesWriteLockedFile` 的契约（FILE_READ_ATTRIBUTES only）真机 PASS 非 Skip |
| W5-5 | 大小写敏感目录（**M36 同族**） | `fsutil file setCaseSensitiveInfo C:\test enable`，目录内放 `a.txt`/`A.txt` 两个不同内容文件后扫描 | **不得折成同一文件**；卷默认判不敏感时，目录级敏感是否被探针捕获→记录（当前 `default_insensitive.go` 的假设风险正是这个形状） |
| W5-6 | 根去重折叠（M26 变异逃逸） | 同时以 `C:\a` 与 `C:\A\B` 为根扫描 | 可重叠根正确折叠、`c:/a/b` 与 `C:\A\B` 折成同键；这是 M26 修向在真机的核对项 |
| W5-7 | ExcludePaths 大小写（**结构性缺口取证**） | `ExcludePaths: ["Cache/Sessions"]`，盘上实存 `cache/sessions` | 当前预期：**不命中**（Windows 上 `Proven` 恒 false → 排除永远区分大小写，域 T-1）。测出即坐实"`GetVolumeInformationW` 补读数（§28.6）应排期" |
| W5-8 | NTFS 压缩/稀疏实占 | 对启用压缩的卷与稀疏文件（`fsutil sparse setflag`）扫描 | `GetCompressedFileSizeW` 给出实占<逻辑；`INVALID_FILE_SIZE` 双向判据不误判 |
| W5-9 | **WIN-1（M72）补码后复测** | 当前代码未 `SetLastError(0)` → 先修（04 §6.11 登记表行首 `\| M72 \| WIN-1 \|` 那一行 + 表尾约束(2)"标'✗'的四条开工即须写"），再复测 W5-8 | 干净 lastError 下不误读 0xFFFFFFFF |
| W5-10 | 占位/缓存第③重证据 | 同文件两种拼写路径（`FILE.TXT`/`file.txt`）重复扫描 + 缓存命中 | 记录 `CtimeNs==0` 是否真机普遍（`internal/fsid/fsid_windows.go` 注释"change time 取不到只丢这一重证据（CtimeNs=0）"、09 §4.3 对用户口径"取不到时只丢这一重证据、记为 0"）、缓存主键两行的实际后果（域 O） |

---

## P5 · ADS 命名流（M32 / M35）

| # | 场景 | 操作方式 | 结果验证 |
|---|---|---|---|
| W6-1 | V8b 端到端 | 从 Internet Explorer/Edge 下载一批文件（自动带 `Zone.Identifier`），执行扫描+动作 | 带命名流文件被守卫**拒绝处理**且报 `Stage "ads"` Failed；症状面确认"大量拒绝"还是"全部放行"（域 G）。前置：A1 的 `-v` 跑法证明该用例没 Skip |
| W6-2 | 默认流放过 | 普通 NTFS 文件（仅 `::$DATA`） | 正常放行，不被误拦 |
| W6-3 | 枚举开销（M34 依赖） | 大语料扫描时记录 ADS 普查前后耗时 | 给 M34"扫描期普查做不做"提供真实开销数 |
| W6-4 | 非 NTFS 卷 | exFAT U 盘上扫描 | `FindFirstStreamW` errno 87 → 分类落 `ErrUnsupported`，不崩不误拦 |
| W6-5 | M35 症状面 | 两个默认流相同、命名流不同的文件 | 当前设计**仍报重复组但动作被拒**——确认界面表现与 09 §6.4 边界注口径一致（"两个默认流相同、命名流不同的文件**仍会被报成重复组**（只是清理被拒，不是没被报重复）"），用户可读 |

---

## P6 · 云端占位文件（OneDrive，M31/M36 族）

| # | 场景 | 操作方式 | 结果验证 |
|---|---|---|---|
| W7-1 | RECALL 位真读数 | OneDrive 设"按需释放空间"，全盘占位状态下扫描 | `skipped_cloud_files` 计数 **>0 且数目==占位数**（若恒 0＝属性位推断错误，整包意义消失）；占位文件不参与哈希 |
| W7-2 | 水合成本 | 打开 `AllowCloudHydration` 扫 OneDrive 目录 | 记录实际下载流量/耗时量级（"扫全盘=拉全云端"风险，域 I），确认文案是否足够劝退 |
| W7-3 | 探针在云目录 | 对占位目录执行大小写探针 | 探针失败→全退默认 + `CaseProbeUnproven` 只报计数不改判（不中止整轮扫描）；若整轮"扫不动"则是域 J 的真机新缺陷 |

---

## P7 · 扫描器 / 性能 / 盘根（#2、M29、域 L/M/Q）

| # | 场景 | 操作方式 | 结果验证 |
|---|---|---|---|
| W8-1 | 盘根扫描保护 | 以 `C:\` 为根扫描 | `pagefile.sys`/`hiberfil.sys`/`swapfile.sys`/`DumpStack.log.tmp`/`$Recycle.Bin`/"System Volume Information"/`WindowsApps` 全部被挡；`WindowsApps` 特权失败产生的 FailedItem 噪声量可接受（域 L）；**ExcludePaths 在盘根下不静默失效**（`internal/scanner/rootprefix_probe_test.go` 中 `TestExcludePathsWithTrailingSeparatorRoot` 头注自认："盘根那一支属 §15.5 的未兑现边界"） |
| W8-2 | 句柄数抽样（#2） | 大语料扫描中记录句柄峰值 | 非候选文件不开句柄（`keyFromInfo` 刻意不解析的取舍成立）；`resolveByHandle` 退化腿只在候选组内出现 |
| W8-3 | realbytes 开销 A/B（M29） | 同一语料分别在带/不带 `GetCompressedFileSizeW` 调用的构建下跑 benchgen，比对阶段 1 耗时 | 差值构成"是否可感知回归"的终判 |
| W8-4 | 介质并发（域 Q） | HDD 外置盘 / SMB 卷上整盘扫描 | Windows `Probe` 恒 Unknown → 维持 NumCPU-1 并发；观察是否出现寻道抖动/超时雪崩，决定是否补 `GetDriveTypeW` 降并发 |
| W8-5 | 中文/特殊字符（#7） | 文件名含中文、emoji、代理对、`\/*?"<>|` 之外的合法特殊组合走全链路（扫→哈希→移动→回撤） | 全链路无损；`buildPathList` 双 NUL 表经真 Shell 消费一次（域 A 遗留：5 条列表用例是纯 Go 编解码，无一条真调 API） |
| W8-6 | 保留名真实触发面 | NTFS 上造不出 `CON.txt`；用 FAT32 U 盘/网络卷/旧数据制造实存保留名文件 | 守卫拦截计数（`protectedFiles`）；确认"根级才挡"的边界不误伤嵌套同名合法文件 |
| W8-7 | `$I`/`$R` 元数据 | 盘根扫描时回收站元数据文件 | 不误入分组（m6 设计稿 `specs/2026-09-21-m6-implementation-design.md` §2.9"本轮不做"登记项："登记为 M7 真机清单项，本机无法实证其 Win32 行为，不做纸面修复"） |
| W8-8 | 短读成因清点 | 压缩卷/稀疏/OneDrive 水合中/HDD 慢读下大批量扫描 | 首扫不再复现"0 组/未保存缓存"（hasher 2026-09-19 事故的同类现象）；`ActualKnown` 作废口径正确 |

---

## P8 · UI / 系统集成 / 数据层（#6、M8、域 N/O/P）

| # | 场景 | 操作方式 | 结果验证 |
|---|---|---|---|
| W9-1 | WebView2 真运行时 | 在 Wails 窗口内（非浏览器 dev）验证：拖拽入库、事件流刷新、原生目录选择对话框 | 三项全部可用（`frontend/src/wails.ts` 的 `backendOrNull` 取不到后端时抛错文案「后端不可用（浏览器开发模式下请使用 Wails 运行）」，明示浏览器不等价） |
| W9-2 | 不可回撤文案 | trash 一条记录，看徽标/title/toast | `undo-code-windows-trash` 文案渲染正确、裸码不漏出（`scan-undo-error` 契约）；"打开系统回收站"落到当前用户 SID 的回收站 |
| W9-3 | M8 呈现位 | 结果页横幅（`unprotectedRoots`）、保留名计数（`protectedFiles`） | 09 §1 产品概述"结果页横幅属后续版本，界面当前不呈现"与云端文件段"界面上目前没有『已跳过 N 个云端占位文件』这条横幅——新增计数的呈现属 M8（前端体验）"两处口径是否仍未呈现；若借本轮开显示，同步改文档 |
| W9-4 | Win32 错误本地化 | 收集一轮真实失败：`FailedItem.Err` 下发内容 | 记录用户实际看到的是 `FormatMessage` 英文/本地化文本还是中文（域 P，全仓零收敛，测出即立账） |
| W9-5 | 注册表三态 | 正常 / HKCU 键被设怪类型（`REG_SZ "1"`）/ ACL 拒绝 三态下预检 | off/unknown/mismatch 分类与真机返回码一致（Linux 上 Errno 2/5 是**数值巧合**，本轮是第一次语义级取证） |
| W9-6 | 配置目录 | 检查 `os.UserConfigDir()` 实际落点；若 `%APPDATA%` 被 OneDrive 重定向 | `cache.db`/`history.db` 不得落在云同步目录（WAL 不可靠风险，域 N）；重定向机器上给出告警或迁移 |
| W9-7 | SQLite 文件锁 | 双开应用 / 杀软扫过 db 文件时读写 | `busy_timeout=5000` 是否够；不出现"database is locked"裸错直达用户 |
| W9-8 | O_EXCL 撞目录 | `claimDst` 抢占目标为"已存在目录"时 | 记录真机返回 EEXIST(183) 还是 EACCES(5)（m6 设计稿 §8.5"未兑现与边界"原话：两者"都会走另名恢复（不覆盖、不报错），**无行为差异**，故不单列 ID，只记边界"；04 §6.9.8 有同文转录，读数回填注释即可） |
| W9-9 | 启动失败痕迹 | 故意制造启动失败（db 损坏等），GUI 子系统下运行 | `main.go` 的 `main()` 启动失败出口（注释原话"P3：println 在无控制台附加的发布版（Windows GUI 子系统）里等于丢弃错误"+ 文案「FileDedup 启动失败」）→ 确认真机上是否留下任何可诊断痕迹，不留则立发布风险账 |

---

## P9 · 发布体系（#9、M120）

| # | 场景 | 操作方式 | 结果验证 |
|---|---|---|---|
| W10-1 | 产物冒烟 | 下载 build.yml 的 windows zip，解压裸 exe 直接运行 | SmartScreen/未签名告警下的首次启动路径；`info.json`/manifest 生效（`check-version-sync` UTF-8 修复后的发布腿首跑复账，M120） |
| W10-2 | 安装/卸载（#9） | 若引入安装器则全链路；当前为裸 exe——至少验证数据目录残留与"卸载"=删目录的行为 | 记录残留清单；同时裁定 04 §6"开放项与风险"**A 组 — 发布前必须由用户决策**第 2 条"是否接受当前不可回撤面"（发布说明是否需前置强调） |
| W10-3 | CI windows 腿超时 | 一次 tag 触发观察 build.yml windows job | timeout-minutes 改动生效（M120 唯一兑现途径） |

---

## P10 · 单实例保护（M196 的未兑现格）

机器证明到的部分：`SingleInstanceLock` 接进了 options（P-1）、两个前置原语都被调且 Unminimise 在前（P-2/P-4）、`a.ctx` 未就绪时不调 wruntime（P-3）、锁标识三腿形状合法（P-6）。CI 三条腿**都不起 GUI**（冒烟走 `fdd-cli`），所以"窗口真的跳到最前"这一整格只有真机读数能兑现（04 §6.36 五按"代码已改、验证未兑现"记账）。机制与三腿差异见 09 §2.1.1。

| # | 场景 | 操作方式 | 结果验证 |
|---|---|---|---|
| W11-1 | 正常双开合并 | 已有一个窗口时，资源管理器里再双击一次 exe（或 `start` 一次） | ① 第二个进程**立刻消失**、不留任务栏项；`start /wait` 时 `%ERRORLEVEL%` 应为 **0**（windows 腿 `os.Exit(0)`，与 linux 腿的 1 不同）；② 前一个窗口被带到最前并取得焦点；③ 窗口内弹出「已合并另一个 FileDedup 实例（它的启动目录：X）」，括号里的目录＝第二实例的 cwd，从不同目录各启动一次以确认它跟着变 |
| W11-2 | 最小化态双开（★ 本批新语义的正面判据） | 先把已有窗口**最小化到任务栏**，再双击 exe | 窗口必须**从最小化恢复并到前台**。若表现为"进程退掉了、任务栏图标闪一下窗口仍最小化"＝`WindowUnminimise` 这一半在真机上不生效——这正是 P-2 要两个原语、且 Unminimise 在前的原因，属 FAIL 不是 SKIP |
| W11-3 | 在途操作期间的合并 | 一批清理执行中（进度条在走）双开 | 提示照常出现，但**在途批次不得被打断或重启**；`opsRunning` 互斥门是进程内的，这道锁失效时（下一条）唯一挡并发的是它，读数要能区分"谁挡住了第二次清理" |
| W11-4 | 放行残余：正向证据 + 尝试复现 | 正向：用窗口枚举工具（Spy++/`EnumWindows`）在**第一个**实例运行时找类名 `wails-app-FileDedup-sic`、标题 `wails-app-FileDedup-siw` 的消息窗口（`single_instance.go:129` 以 `HWND_MESSAGE` 为父创建，不在任何桌面窗口列表里，只能按类名/标题找）。复现：想办法让"互斥体报已存在但 `FindWindowW` 取回 0"（`single_instance.go:70` 自陈此时**照常起第二个实例**） | 找到消息窗口＝合并链路的接收端真实存在；复现不出即记 **SKIP + 原因**，不得写成"已验证不会放行"。**预期就是复现不出**：互斥体名不带 `Global\` 前缀（`id+"sim"`，会话内命名空间）⇒ 与消息窗口要么同在同一无、要么根本走不到 :70 |
| W11-5 | 保护覆盖面边界：跨账户双开 | 第一个实例正常启动后，用"以其他用户身份运行"再启动一次（同一台机器、不同登录会话） | **预期起两个独立窗口**（会话内互斥体与消息窗口都不跨会话 ⇒ 这不是 :70 那条放行，而是保护天然不管跨用户）。坐实后按"覆盖面边界"记进 09 §2.1.1 口径，**不记成缺陷**；真正要防的是两个人共用一份 `history.db`/同一批文件——记录该场景下并发清理会不会互踩 |
| W11-6 | 锁的残留与并发启动竞态 | 同一账户下用两个终端各 `start` 一次、连起 5 个实例 | 只应活下来 **1** 个窗口；其余全部退出。若活下 2 个以上属真机新缺陷（互斥体抢锁竞态），带 OS 版本与 AV 状态回报 |

★ **macOS 腿的同一格不在本文范围**：本文标题与口径只覆盖 Windows（§0.1 的状态口径、§0.3 的环境要求全是 windows 语境）。macOS 侧有两格 Windows 取不到：`$TMPDIR` 不可写时的**静默不启动**（09 §2.1.1/§10 已给用户排查口径，但从未真机复现过），以及 linux 腿 `os.Exit(1)` 与"回调可能早于 `a.ctx` 就绪"的时序。收录它们需要先把本文扩成三腿清单——**本批未做，属另案**。〔2026-09-24 **M209 已兑现这个"另案"**：本文已改名 `05-真机实测清单.md` 并扩成三腿，上面点名的两格现读 **MAC-1 / MAC-2 / LNX-5**。这一段原文一字不改地留着，是为了让"上一批为什么没写"这件事本身留在册子上；它的现行效力以上一句为准。〕

---

## P11 · 缓存隔离重建与真锁文件句柄（M213 的未兑现格，windows 腿）

`6de2a49` 把 `dbfile.Quarantine` 改成"要么完整隔离、要么原地不动"（主影像 + `-wal`/`-shm`
同进同退，回滚失败话术点名"勿在原路径重建"）。CI 三腿跑的是**接缝注入**判据
（`renameFile` 缝上指定后缀返回失败）；"另一进程真正握着 `-wal` 句柄导致 rename 失败"的
Windows 真锁形态**没有读数**——与 W9-7（SQLite 文件锁）同场可测。

| # | 场景 | 操作方式 | 结果验证（判据） |
|---|---|---|---|
| W12-1 | 真锁 `-wal` 下的隔离-回滚全链 | Windows 真机：让缓存库处于损坏状态（照 `test-windows-quarantine.sh` 的造法）**且**另起进程写打开 `-wal` 不放（或挂杀软实时扫描制造 sharing 拒绝），触发一次重建 | ① 隔离失败时报的是"隔离失败…放弃重建"这条可读错，**不是**半边隔离后旧库照常打开；② 盘上终态二选一：三件套（db/-wal/-shm）**全部原地**，或全部进 `.broken-*`——不得出现"主影像进了隔离名、-wal 留在原地"的残躯；③ 若撞上"主影像已隔离且无法挪回"的话术格，按提示人工核实隔离影像后，原路径**不自动**重建；④ 全程 `history.db` 不受波及 |

---

## MAC · macOS 真机格（darwin 腿，M209 新增）

在册出处：04 §6.8.9 三"本轮仍未兑现的证据清单"的 macOS 条目、04 §6.36 五的 ★ 格、09 §2.1.1 末段那条排查口径（原文"系统临时目录不可写时，应用会静默不启动"）、本文 P10 末尾 ★ 注（"从未真机复现过"）、04 的 M31/M51/M84 三行兑现边界。设计段 `specs/2026-09-24-checklist-three-leg-design.md` §4。

Wails 侧坐标统一取模块缓存 `github.com/wailsapp/wails/v2@v2.16.0/internal/frontend/desktop/darwin/`（本批现读；按 M150 口径只钉符号不钉行号）。

| # | 场景 | 操作方式 | 结果验证（判据） |
|---|---|---|---|
| MAC-1 | 单实例合并（W11-1 / W11-2 的 darwin 对格） | ① 从终端启动一次，再在 Finder 里双击第二次；② 把已有窗口**最小化到 Dock** 后再双击第二次；③ 全程另开一个终端 `ls -l "$TMPDIR/FileDedup.lock"` 盯锁文件 | ① 第二实例**立刻退出**且 `echo $?` 为 **0**（darwin 腿 `SetupSingleInstance` 走 `os.Exit(0)`，与 linux 腿的 1 不同）；② 窗口被带到最前并取得焦点——darwin 的前置**只挂在 `Show`（`makeKeyAndOrderFront` + `activateIgnoringOtherApps:`）上**；③ ★ 最小化那一格必须**从 Dock 里恢复出来**：darwin 的 `UnMinimise` 才是 `deminiaturize:`，如果表现为"进程退掉、Dock 图标弹一下窗口仍在 Dock"＝两个原语里少起了一个作用，属 **FAIL 不是 SKIP**（这正是 `raiseMainWindow` 要两个调用、且 Unminimise 在前的原因，见 P-2）；④ 窗口内提示"已合并另一个实例（它的启动目录：X）"，X 随第二实例的 cwd 变；⑤ 锁文件残留无害性：`kill -9` 掉首实例后再启动，必须**正常起**（`flock` 问的是谁持有，不是文件在不在）——测出"残留 .lock 导致起不来"即真机新缺陷；⑥ 跨账户双开（同 W11-5 族）：darwin 的锁文件在各自的 `NSTemporaryDirectory()` 下 ⇒ **预期两个独立窗口**，按覆盖面边界记，不记成缺陷 |
| MAC-2 | `$TMPDIR` 不可写 ⇒ **静默不启动** | 造一个不可写的临时目录，从终端把可执行文件在它下面启动一次：`D=$(mktemp -d); chmod 500 "$D"; TMPDIR="$D" FileDedup.app/Contents/MacOS/FileDedup; echo $?`。★ 先 `ls -l "$D/FileDedup.lock"` 核对锁文件**确实**落在这个目录；没落过去说明这一版 `NSTemporaryDirectory()` 不认外部 `TMPDIR`，改用"把真实 `$TMPDIR` 先 `chmod 500`"那一法，并把用的是哪一法记进读数 | 预期症状是 Wails 侧现读的**推论**，本格要取证的就是它：`createLockFile` 把"打不开文件"与"锁已被别人占"合在同一个 `err != nil` 出口，而该出口一律当作"已有首实例"处理 ⇒ 发通知 + `os.Exit(0)`。所以真机上应看到：**没有窗口、没有错误框、退出码 0**。唯一痕迹是标准输出上那行 `Failed to open lockfile …`（从 Finder 启动看不见 ⇒ 必须走终端）。恢复权限后应正常启动。★ 若真机上"其实有一个首实例在跑"与"临时目录不可写"**两种情形不可分辨**（症状完全相同），这就是 09 §2.1.1 末段与 §10 那条排查口径的唯一取证点；测出的措辞若比手册更悲观，回填 09 |
| MAC-3 | 以 `/` 为根真扫的耗时与噪声（M84 拆分的代价格） | 真机上跑 `time ./fdd-cli -o /tmp/rootscan.json /`（`-hidden` 开/关各一次），GUI 侧再以 `/` 为根扫一遍对照 | ① **必须被扫到**：`/private/var/folders`、`/private/tmp`——`sysguard` 表里这两处**刻意不在册**（M84 裁定原话"那两处装的是用户自己的缓存与临时文件，是真实的可去重数据"），扫不到＝清单把 M84 又吞回去了；② **必须被剪**：`/System` 与八条 `/private/...` 后代（etc / var/db / var/log / var/root / var/spool / var/at / var/empty / var/run），以及 `.Spotlight-V100` / `.fseventsd` / `.DocumentRevisions-V100` / `.com.apple.TimeMachine-*.snapshots`；③ JSON 里 `protectedDirs`/`protectedFiles` 给出真实计数，`unprotectedRoots` **应为空**（逃逸判据要求"用户点名的根落在保护目录内部"，`/` 不落在 `/System` 里）——非空即新缺陷；④ 记 total 耗时、文件数、`failed` 条数与内存峰值：04 的 M84 兑现边界原话是"本机没有做一次全量真扫的耗时读数 ⇒ 手册已按新判据措辞，**代价未实测**"，本格就是那句"未实测"的销账点。★ 若耗时不可接受，回到 04 另立 ID 讨论"根=盘根时的额外剪枝"，**不改写** M84 行 |
| MAC-4 | dataless 占位的**非 0 字节**正例（M31） | **需用户在场并同意下载配额**（这一前提是 M31 登记行原文写死的，不是清单自己加的）：在一次性 iCloud Drive 子目录放两个**非空**"仅保留在线副本"的文件，跑 `./fdd-cli -o /tmp/cloud.json <该目录>`，**不加** `--allow-cloud-hydration` | ① `skipped_cloud_files > 0` 且数目==占位数（04 的 W7-1 同族判据在 darwin 的对格）；② 该两个文件**一个字节都没被下载**（观察 iCloud 流量/`brctl` 状态/文件仍是占位）——"识别出 2 个、语料里少 2 个、一个字节都没下载"正是 M31 点名的那句最有说服力的断言；③ 打开 `--allow-cloud-hydration` 复跑，记录实际下载量级（水合成本，同 W7-2 族）。★ 现状是"取样 3、dataless 2，全是 0 字节 `.localized`"——那批样本证明的是"位可读、可二分"，**没有**证明"省下一次真实下载"；本格补的正是后半句 |
| MAC-5 | darwin `osascript` 分批/超时/行协议三格（OPS-8 族，登记行 M51） |真机 + 真实废纸篓，三件事各一次：① 一批 **>100 条**（`trashBatchSize`）的移入废纸篓；② 让**后一批**失败（例如第 2 批里放一个 root 属主、当前用户无权移动的文件，或让 Finder 弹出授权/无响应）；③ 文件名含**换行符**（`touch $'bad\nname.txt'`）与一个正常文件混一批 | ① 分批数与"此前批次已入站"的口径成立：前 100 条必须已在废纸篓且**账本记到去向**（`defaultTrash` 现读是 `return dst, err` 把部分 dstMap 一并交回，executor 的 C6 逐文件回退再补）；② 每批 2 分钟超时（`trashBatchTimeout`）+ `WaitDelay` 5s：Finder 无响应时报的是**"osascript 超时（>2m0s，第 i/n 批 …）"**这条可读错误，而不是整轮卡死或无限等待；③ 含换行的名字**只丢自身一条**（行协议 `src␟dst` 被破坏），该条按"去向未知"入账（`DestPath` 空 → 回撤给出明确错误），**不得**连带把同批其它文件的落点一起丢；④ 全程不得出现"报成功但文件既不在原位也不在废纸篓"。★ 措辞提醒：M51 登记时的危害（"批量失败丢全部 dstMap ⇒ C2 修复在 darwin 腿失效"）现读已被上面 ① 那两半**部分**消解；按本仓口径**不改写 M51 行**，本格只测"今天还没有读数的三件事"，测出的差异回填 04 新行 |
| MAC-6 | darwin 以 `-` 开头/含 `-i` 文件名走**真 App**入废纸篓（M211 的真机格） | 真机 + 真实废纸篓，经 GUI 而非直发 osascript：`touch -- -dash.txt` 与一个正常文件混批执行"移入废纸篓"；再把文件命名为 `-i`（同样混批）重复一次 | ① 两批都**全量**进废纸篓、账本逐条记到去向——M211 修前形状是 `-i` 那批 rc=0 零输出、executor 整批记 done 的**虚账**，修后由 `--` 终止符 + 输出配对防线双钉住；② 本机 `38a7088` 批已在测试里**真发 osascript** 拿到判据读数（`TestOsascriptArgsDashTerminator`），本格补的是"经 Wails 窗口与 Finder 全链路"那一层：徽标/失败清单/回撤入口对这两个文件的呈现与真实落点一致；③ 回撤后文件回到原位且名字一字不变 |

---

## LNX · Linux 真机格（linux 腿，M209 新增）

在册出处：04 §6.8.9 三的 Linux runner 挂账（"新增的 6 个候选卷一个都没取证"）与 coreutils 契约挂账、04 §6.29 五-1（XDG 真跨卷形态）、04 的"M84/M28/M55/遍历键不折叠"四处兑现边界、ci.yml 头注"尚未纳入"第 3~4 条。

| # | 场景 | 操作方式 | 结果验证（判据） |
|---|---|---|---|
| LNX-1 | 七个跨卷候选卷逐个点名（G9） | **自己那台** Linux 机器上 `go test ./internal/realbytes -run TestVolumeIDDistinguishesMounts -v`，抄下日志那行"**跨卷对照成立：\<谁\>**（dev=…）vs 临时目录（dev=…）"。多台机器各跑一次。★ 别指望现成的 CI 产物：三腿的 `go test` 都不带 `-v`（§0.1 第 1 条 / 04 的 **M100**），2026-09-24 现读 run `35966760555` 三腿日志里 `跨卷对照成立` **零命中**——ubuntu 腿虽有真 loop 卷（§0.1 第 3 条），那行点名日志被吞。要么本地带 `-v` 跑，要么等 §0.2 的 A1 落地后从 CI 读 | ① 该用例**报 PASS 且点名了是七个候选里的哪一个**（不 Skip）；② 目标是让 `/dev/shm`、`/dev`、`/run`、`/tmp`、`/proc` 各在**至少一台**机器上被点到——04 挂账的原话是"本机 darwin 只用得上 `/dev/null`，新增的 6 个候选卷一个都没取证"，只跑一台只会销掉其中一个；③ 容器/单卷 overlayfs 上若"全部候选与临时目录同卷" ⇒ 用例按设计 Skip，此时**必须**记 SKIP + 该机器 `$TMPDIR` 的挂载来源（这条 skip 分支本身就是"CI 上等于没有证据"的那个形态，见 `crossVolumeCandidates` 注释原话）。★ 注意取的是**第一个 dev 不同的候选**，不是全部：一条日志只销一个名字 |
| LNX-2 | XDG **真**跨卷、`rename` 真报 EXDEV 端到端 | 造真两卷：第二块物理盘，或 `img=$(mktemp); truncate -s 2G "$img"; mkfs.ext4 -f "$img"; sudo mount -o loop "$img" /mnt/vol2`。把 `XDG_DATA_HOME` 指到**与目标文件不同卷**的目录（`XDG_DATA_HOME=/mnt/vol2/xdg ./FileDedup` 或同环境下的 `fdd-cli`），对另一卷上的文件执行"移入回收站" | ① 走的是复制腿：耗时与文件大小成正相关、目标卷占用**增加**、源卷占用随后**下降**（删源成功）；② `<XDG>/Trash/files/` 与 `Trash/info/` 成对出现，`.trashinfo` 的 `Path=` 是**原始绝对路径**（不是 `file:///`——H4 修的就是这个）；③ DE 的回收站（GNOME Files / Dolphin）看得见该条目并能还原回原路径；④ 全程 `df` 上不允许出现"两边都还在"或"两边都没了"的中间态。★ 为什么 CI 不算：测试里的"跨卷"由 `forceCrossVolumeRename` **注入** EXDEV（`move_crossvolume_identity_test.go`），04 §6.29 五-1 那句"只有 Linux 真机/CI 腿能给端到端读数"里的 **CI 半边今日现读只兑现了"有真卷"，没兑现"端到端"**——ubuntu 腿确实真挂了 loop + ext4（见 §0.1 第 3 条那条更正），但它跑的是 `smoke-symlink.sh` 那七条**文件系统语义**判据，回收站复制腿（`copy + .trashinfo + 删源`）在 CI 上从未在真卷上走过 ⇒ 本格仍是要素齐全的一格。★ 环境代价因此比初稿写的低：**不必另找第二块物理盘**，在同一条 loop 卷上把 `XDG_DATA_HOME` 指过去即可；可选加测：复制窗口内用第三方 `mv` 顶掉源 ⇒ 守卫必须拦下、**不删任何路径**、副本+trashinfo 留存（§31 判据的真机版；时机太难掐就记 SKIP + 原因）。★ 另一可选加测（**B4 / R-操作-2** 的真机版，与上面 §31 守卫**取向相反**）：让删源稳定失败——把源文件放进一个 `chmod 500` 的父目录（用户仍可读文件 ⇒ 复制腿成功，但无目录写权限 ⇒ `unlink` 报 EACCES），对其执行跨卷移入回收站 ⇒ 判据是 ①源原封不动、②`Trash/files/` **不得残留**那份已复制好的副本（`moveIntoTrash` 删源失败即 `os.Remove(dst)`）、③`Trash/info/` 同步无残留 trashinfo、④报"删源失败（源未动、回收站无残留副本）"而非虚账 done。与 §31 那支的区别正在源的身份：被顶替时副本是唯一可还原的留存**必须保**，删源失败时原件好端端在 src、副本纯属冗余**必须清**（`trash_xdg_orphan_b4_test.go` 已在 darwin 用注入 removeSrc 失败真跑过判据，本格补的是真 EACCES 那一层；构造不出就记 SKIP + 原因） |
| LNX-3 | 大小写**不敏感**卷挂在 Linux 上（遍历键不折叠的反向档） | 拿一个 vfat/exfat U 盘、NTFS-3G 卷，或 `mkfs.ext4 -F -O casefold` + `mount -o casefold` 的 loop 卷，在上面：① 造 `notes.txt` 与 `NOTES.TXT`（同卷无法并存时，改造**不同内容**的 `Report.pdf` 与 `report.pdf`，看盘上最终剩几个）；② 以该卷上两个大小写拼写不同的目录作根各扫一遍；③ 配一条 `ExcludePaths`，盘上实存另一种大小写拼写 | ① 不敏感卷上两种拼写**指同一文件**，不得被当成两个条目配成"自己跟自己是重复组"；② 遍历期**不折叠**的既有口径（04：遍历键不折叠、折叠只服务根去重那一问）在卷语义与盘实际敏感度不一致时不产生漏扫/重扫；③ `ExcludePaths` 命中与否**必须**与 `Proven` 探针结论一致——探针判不敏感时排除应大小写不敏感，判不出时 `CaseProbeUnproven` 计数要报出来（域 T-1 那一族在 linux 上的对格）。★ 收窄说明：04 的在册原话是"Windows / Linux 真机读数未取"，但同一批的"兑现边界 1"明写敏感卷那一档"**默认兑现环境是 Linux CI**" ⇒ 同卷敏感形态 CI 已有读数，本格只补**上面这个反方向**，别重复索债 |
| LNX-4 | NFS/FUSE"恒报 0" + 未解析卷上的链接身份（M28 / M55） | 挂一个 sshfs 与（可得时）一个 NFS 导出，各跑一次 `./fdd-cli -o /tmp/nfs.json <mnt>`。语料：`truncate -s 1G /mnt/sparse.bin` 造**真全洞**，另放一个**非空洞**小文件当卷内证据；`stat -c '%b %B' <file>` 与 `df` 记原始数值。再在卷上建一个**用户自己**的软链接指向卷内文件 | ① 记 `st_blocks` 在该文件系统上**报什么**（0？真值？）——04 的原话是"真实 sshfs/s3fs 报什么，本轮没量过"；② 该卷上没有任何非空洞证据时 ⇒ `reclaimable_actual_known=false` 且显示"未知"，**既不许报 0，也不许拿逻辑大小冒充实占**（`From` 规则 ②/③ 的真机对格）；③ 同卷放了非空洞文件后（`proven` 腿）⇒ 真全洞那个应报 `实占 0 / known=true`；④ ★ M55 那一格：卷上 `fsid` 解析不出来时，**清理/合并**动作不得把用户自建软链接当成我们的残留删掉（`slotProvesSymlink` 在 `!Resolved` 时的 fail-closed 口径，登记行原话"△ 本机无 NFS 腿"）；⑤ 挂不上任何网络文件系统 ⇒ SKIP + 原因，**不得**用本地卷读数替代（这一格测的就是那个环境形态本身） |
| LNX-5 | 单实例的 linux 三格 | A) **有会话总线**：桌面会话里终端启动一次，再启动第二次（`echo $?` 看码）；把首实例**最小化**后再第二次启动。B) **无会话总线**：`env -u DBUS_SESSION_BUS_ADDRESS ./FileDedup` 起两次（或纯 tty / 远程会话里起）。C) **竞态**：让第二实例在首实例的 `OnStartup` 之前抢到，并在首实例的终端上 grep 那句"启动未完成时收到第二实例信号" | A① 第二实例退出码应为 **1**——`SetupSingleInstance` 的 linux 腿是 `os.Exit(1)`，**三腿里只有它不是 0**，手册/脚本引用别写错；A② 最小化态必须恢复并前置：linux 的前置**只挂在 `WindowUnminimise`（`gtk_window_present`）上**，若表现为"进程退掉、窗口仍最小化"＝P-2 那一半在真机不生效，属 **FAIL 不是 SKIP**；A③ 提示弹在首实例窗口。B) 无总线 ⇒ **两个窗口都起**：这一格是坐实 09 §2.1.1 给用户的那句"这一腿等于零保护"，测出**两个窗口**才是符合文档的读数（不是缺陷、也别写成"保护生效"）。C) 构造不出来就记 **SKIP + 原因**——`a.ctx` 零值守卫（P-3）在 linux 腿的可达性靠的是"`Run` 里 `go OnStartup` 与抢锁并发"这一时序，人手掐不准；不得写成"已验证不会漏" |
| LNX-6 | 真挂死 ⇒ GNU `timeout` 给 124（AS-K5 契约） | 装了 GNU coreutils 的机器上：`timeout --signal=TERM --kill-after=30s 3s bash -c 'trap "" TERM; sleep 999'; echo $?` → 期望 **124**；再用同一条前缀跑一次真脚本（`timeout … 5s bash scripts/smoke-symlink.sh` 之类会阻塞的形态）观察是否留下可读原因 | ① rc=124（本机 darwin 无 GNU `timeout`，04 挂账原话"只演示了受控退出码"，真挂死这一格从未有过）；② `trap "" TERM` 那一档必须被 `--kill-after` 兜住而不是无限期挂；③ 与 CI 那一格对齐：`ci.yml` 里"跨卷软链接冒烟"步骤按码分流，`124\|137\|143` 三个码判**失败**并写进作业摘要，不会被降级成"跳过"。★ 这一格属**门禁契约**、不是产品功能，读一次即可销账；busybox 版 `timeout` 不算（rc 与 `--kill-after` 语义不同） |

### 跨腿共格提示（不改写任何 W 段历史行）

- **W4-1 / W4-2** 的"复制 + 校验 + `identityStill` + 删源"记账腿：Linux 真机的对格是 **LNX-2**（真两卷 + 真 EXDEV），另加 §31 守卫的真机版可选项。
- **W5-5 / W5-6**（大小写敏感目录、根去重折叠）：Linux 对格是 **LNX-3**（不敏感卷那一档）；darwin 的副读数落在 **MAC-3** 的同一次真扫里。
- **W8-5**（中文/emoji/特殊字符全链路）与 **W8-1**（盘根扫描保护）属**三腿共格**：判据同一条，各腿执行时把读数回填到 W 段对应行的"Darwin/Linux 附注"里，本批**不另立 ID**（同一判据三处各写一遍是漂移的开始）。

---

## 附：回填约定

- 每条读数按来源编号回填 04（M7 总账 ⊃ E 组回收站/软链接、T04、M26/M29/M31/M32/M33/M35/M36/M45/M72/M100、§3.5 #2/#4/#6/#7/#9、AS-R4）。〔2026-09-24 M205 勘误：旧版此处另有三个裸行号 `:575`/`:1058`/`:1127`，在 04/设计稿中已随批增行号漂移、逐条现读不可达，改为本清单 W5-10（`fsid_windows.go` 注释 + 09 §4.3）、W1-8（04 §6.6.4"仍可能失败"段）、W9-8（m6 设计稿 §8.5 / 04 §6.9.8）三处锚点引用。〕〔2026-09-24 **M209** 追加：`MAC-*` 回填 04 的 M31/M51/M84 三行兑现边界与 §6.8.9 三清单，`LNX-*` 回填 §6.29 五-1、§6.10 五-1、登记表 M28/M55 行与 §6.8.9 三的 coreutils 条目。〕
- SKIP 必须带原因与解除条件；**"清单项目全部执行完"的定义是每条都有读数（PASS/FAIL/SKIP+原因），不是"看起来绿"**。
- 与真机无关的三个先行项（§0.2 A1/A2/A3）建议在本轮开测前合入，否则 P0/P5 半数项目拿不到差分读数。
- **CI 腿绿一律不折算成真机读数**（§0.1 第 1、3 条）：三腿都不起 GUI、不碰真实回收站、macOS 腿没有 iCloud 账户，`MAC-*`/`LNX-*` 与 `P0~P10` 同受这一条约束。

