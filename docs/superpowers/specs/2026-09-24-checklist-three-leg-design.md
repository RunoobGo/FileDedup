# 设计段：`docs/05` 由 Windows 单腿清单扩成三腿真机清单（M209）

日期：2026-09-24　占号：**M209**　依据：04 §6.36 五的 ★ 格与 05 P10 末尾那条 ★ 注

## 0. 本批从哪来

上一批（M196 单实例保护）在 05 补了 P10 的六条 Windows 格（W11-1~W11-6），并在同一段末尾写下：

> ★ **macOS 腿的同一格不在本文范围**：……收录它们需要先把本文扩成三腿清单——**本批未做，属另案**。

04 §6.36 五按同一口径挂了那一格。用户 2026-09-24 对"另案怎么走"给出的裁定是**扩 05**，随后两条子裁定：

| 子裁定 | 取向 | 对本批的约束 |
|---|---|---|
| 收录范围 | **连 04 在册格一并收**（不只补单实例的 darwin/linux 对应物） | 每条格都要有 04 的在册出处，且**动手前逐条现读**确认它今天仍未兑现 |
| 文件名 | **改名去掉 Windows 字样** | `git mv` + 同步 `docs/README.md` 的两处引用 |

本批是纯文档批：产品码零改动、测试零改动、断言一字不减。

## 1. 收录判据（三条同时成立才上清单）

1. **在册**：04 里有明确出处行（§6.8.9 三"本轮仍未兑现的证据清单"、各批"兑现边界"段、或 §6.11 登记表行）。
2. **现读仍未兑现**：本批逐条现读源码与 04 现文核对，凡已被后续批次收掉的**不再列**（见 §5 不收录清单）。
3. **真机构造得出来**：写得出具体命令或环境前提。写不出的属"不可构造"，只能记成边界而不是清单格（例：M48 那类"钩子是唯一取证手段"的窗口）。

反面纪律同 05 §0.1：**CI 腿绿不折算成真机读数**。三腿的 job step 现读（`.github/workflows/ci.yml`）里
没有任何一步启动 GUI——ubuntu/windows 跑 `go test` + `fdd-cli` 冒烟，macos 同配方，跨卷冒烟刻意只在
ubuntu 跑（ci.yml 头注"尚未纳入"第 3、4 条自认："macOS/Linux 真实回收站与 GUI 端到端：需人工或桌面
会话""macOS 侧跨卷冒烟：harness 依赖 losetup/mount -o loop，仅 Linux 可用"）。所以 GUI 格、真实回收站格、
真跨卷形态格在**三腿上都是零读数**，darwin/linux 与 windows 在这一格上处境相同——这正是本批扩写的立足点。

## 2. 改名与引用面（现读）

- `git mv docs/05-Windows真机实测清单.md docs/05-真机实测清单.md`。
- 标题行、定位段、§0.1、§0.3 的 Windows 专属口径改成三腿口径；P0~P10 的既有条目**一字不改**（它们本来就是 Windows 格，改标题不改内容）。
- 全仓引用面现读：`grep -rn "05-Windows"` 只有 `docs/README.md:12` 与 `:32` 两处，`docs/archive/` 零命中。
  其余文档（04 §6.29/§6.36、09、M197 设计稿）一律用 `docs/05` 这种**与文件名无关**的写法，改名不产生新断链。
- 顺带修一处上一批留下的结构缺陷：`7625d1e` 插入 P10 段时把 `## 附：回填约定` 这一行标题**吃掉**了
  （`git show 7625d1e -- docs/05…` 现读：`-## 附：回填约定` / `+## P10 · 单实例保护…`），
  导致那三条回填约定悬在 P10 的 ★ 注下面。本批补回标题，并把新增两节放在它之前。

## 3. §0.1 / §0.3 的三腿改写

- **§0.1 加第三条基础事实**：CI 三腿都不起 GUI（出处见 §1），并逐腿点名"哪三格在真机之前谁都给不了读数"：
  GUI 前置/合并、真实回收站（`$Recycle.Bin` / 废纸篓 / `~/.local/share/Trash`）、第二物理卷上的真 EXDEV。
- **§0.3 拆成三小节**（Windows 原样保留为 W 段，新增 M/L 两段），每段只写环境前提不写猜测：
  macOS 需要真机（有 GUI 登录会话；iCloud 格另需已配置账户且**用户在场同意配额**）、
  Linux 需要桌面会话 + 至少两个不同文件系统的挂载点（loop 卷即可），并注明"极简桌面/远程会话可能
  连 D-Bus 会话总线都没有"这一格本身就是 LNX-5 的被测对象。
- P0~P10 之前加一行提示：以下 P 段属 **W（Windows）腿**；darwin 见 `## MAC`、linux 见 `## LNX`。

## 4. 新增两节的格子（逐格在册出处与判据）

坐标一律**锚点式**（04 §6.11 的 M150 口径；本文件自身在 M205 一批里刚把行号式引用换成锚点，
不能再往里加裸行号）。模块缓存路径写成 `<wails v2.16.0>/internal/frontend/desktop/…` 形式。

### `## MAC` · macOS 真机格

| # | 格 | 在册出处 | 判据要点 |
|---|---|---|---|
| MAC-1 | 单实例合并（W11-1/W11-2 的 darwin 对格） | 05 P10 ★ 注、04 §6.36 五 | 第二实例退出码 **0**（darwin `SetupSingleInstance` 走 `os.Exit(0)`）；前置靠 `WindowShow`（`makeKeyAndOrderFront`+`activate`），最小化态必须恢复；锁文件现读 `$TMPDIR/FileDedup.lock` 的实际落点与残留后果（`flock` 只问持有者，不问文件在不在 ⇒ 残留**应当**无害，测出有害即新缺陷）；跨账户双开按 W11-5 同族记录（锁文件在各自 `NSTemporaryDirectory()` 下） |
| MAC-2 | `$TMPDIR` 不可写 ⇒ **静默不启动** | 09 §2.1.1 末段那条排查口径（"系统临时目录不可写时，应用会静默不启动"）+ 本文 P10 ★ 注"从未真机复现过" | `createLockFile` 把"打不开文件"与"锁被别人占"**合在同一个 `err != nil` 分支** ⇒ 无首实例时也 `SendDataToFirstInstance` + `exit(0)`：无窗口、无报错、rc=0。唯一痕迹是 stdout 的 `Failed to open lockfile …` ⇒ 必须从终端启动才看得见。恢复权限后应正常启动 |
| MAC-3 | 以 `/` 为根真扫的耗时与噪声 | 04 的 M84 兑现边界格（"本机没有一次全量真扫的耗时读数 ⇒ **代价未实测**"） | `sysguard` 现读：`/private/var/folders` 与 `/private/tmp` **刻意不在册**（M84 拆分），故这两片必须被扫到；`/System` 与八条 `/private/...` 后代必须被剪；以 `/` 为根时 `UnprotectedRoots` **应为空**（逃逸判据要"根落在保护目录内部"，`/` 不落在 `/System` 内）——非空即新缺陷 |
| MAC-4 | dataless **非 0 字节**正例 | 04 的"macOS 正例只有 0 字节样本（取样 3、dataless 2，全是 `.localized`），没有证明省下一次真实下载"；M31 行的"可兑现形状"原话 | 一次性 iCloud Drive 子目录放两个非空占位（**需用户在场并同意下载配额**，这是 M31 登记时就写死的前提）；判据是 `skipped_cloud_files>0` 且该文件一个字节都没被下载 |
| MAC-5 | darwin `osascript` 三格真机行为 | 04:OPS-8 登记表行（M51，"✗ 需真机与真实回收站"）、§五"OPS-8 的 darwin osascript 腿本机**构造不出来**" | ① 按 100 条分批、每批 2 分钟超时 + `WaitDelay` 5s：Finder 无响应/授权弹窗时报"超时"而不是整轮卡死；② 后续批失败时"此前批次已入站"的口径成立（`defaultTrash` 现为 `return dst, err` 一并交回 + executor 的 C6 逐文件回退）；③ 文件名含**换行**破坏 `src␟dst` 行协议时只丢自身一条、账本 `DestPath` 空、回撤给明确错误。★ 说明：M51 登记时的危害（"批量失败丢全部 dstMap"）现读已被 ② 那两半**部分**消解——按 M209 口径**不改写 M51 行**，本格只测今天还没读数的三件事 |

### `## LNX` · Linux 真机格

| # | 格 | 在册出处 | 判据要点 |
|---|---|---|---|
| LNX-1 | 七个跨卷候选卷逐个点名 | 04 §6.8.9 三"Linux runner：G9 的跨卷候选卷列表，新增的 6 个候选卷一个都没取证" | 带 `-v` 跑 `TestVolumeIDDistinguishesMounts`，抄下日志里"跨卷对照成立：<谁>"点名的那一个；目标是 `/dev/shm`、`/run`、`/tmp`、`/proc`、`/dev` 各在**至少一台**机器上取到。全部同卷 ⇒ SKIP + 记录该机器 `$TMPDIR` 的挂载来源（单卷 overlayfs 容器就是这一形态） |
| LNX-2 | XDG **真**跨卷 / `rename` 真报 EXDEV 端到端 | 04 §6.29 五-1（"这一环境形态只有 Linux 真机/CI 腿能给端到端读数"）+ `forceCrossVolumeRename` 现读为**注入式**夹具 | 真跨卷（第二物理卷或 loop 卷）+ `XDG_DATA_HOME` 指过去：走复制腿、`Trash/files` 与 `Trash/info` 成对、`Path=` 指向原绝对路径、DE 回收站看得见并可还原、源卷占用下降。★ 与 CI 的差分：CI 上"跨卷"是钩子造的 EXDEV，**不是**真两卷 |
| LNX-3 | 大小写**不敏感**卷挂在 Linux 上 | 04 的"Windows / Linux 真机读数未取：遍历键不折叠"——★ 本批现读收窄：同卷敏感形态**已有** CI 读数（该批"兑现边界 1"原话"默认兑现环境是 Linux CI"），真机补的是**反方向**那一档 | vfat/NTFS-3G/Android 卡（或 `mount -o casefold` 的 ext4）上：`a.txt`/`A.txt` 无法并存时扫描与根去重各自表现、`ExcludePaths` 大小写是否与卷的敏感度一致、`CaseProbeUnproven` 计数是否报出来 |
| LNX-4 | NFS/FUSE 恒报 0 + 未解析卷上的链接身份 | 04 的"NFS/FUSE'恒报 0'的真机读数未兑现：本机没有可挂的服务端"；登记表 M28 行、M55 行（"△ 本机无 NFS 腿"） | 挂 sshfs 与 NFS 各一：`truncate -s 1G` 稀疏文件上记 `st_blocks` **原始数值**；恒 0 ⇒ `reclaimable_actual_known=false` 且显示"未知"（既不许报 0 也不许拿逻辑大小冒充）；同卷有非空洞文件时真全洞应报 0（`proven` 腿）。★ 另一判据：卷上放用户自建软链接，`fsid` 未解析时**不得**把用户链接当我们的残留删掉（M55 的 fail-closed 对格） |
| LNX-5 | 单实例的 linux 三格 | 05 P10 ★ 注、09 §2.1.1"没有 D-Bus 会话总线时整段跳过 ⇒ 这一腿等于零保护" | A) 有会话总线双开：第二实例退出码 **1**（三腿里只有它不是 0）、前置只挂在 `WindowUnminimise`（`gtk_window_present`）上 ⇒ 最小化态必须恢复、提示弹在该窗口；B) 无会话总线（`env -u DBUS_SESSION_BUS_ADDRESS`、纯 tty/远程会话）双开 ⇒ **两个窗口都起**，坐实手册给用户的那句；C) 回调早于 `a.ctx` 就绪那一格（linux 是 `go OnStartup` 与抢锁并发）：在 A 的 stderr 上 grep 那条"启动未完成时收到第二实例信号"，**构不出即 SKIP + 原因**，不得写成"已验证不会漏" |
| LNX-6 | 真挂死 ⇒ `timeout` 给 124 | 04 §6.8.9 三"coreutils 契约：AS-K5 的'真实挂死 → 124'（本机无 GNU `timeout`，只演示了受控退出码）" | 装了 GNU coreutils 的机器上用桩（`bash -c 'trap "" TERM; sleep 999'`）跑同一条 `timeout --signal=TERM --kill-after=30s` 命令 ⇒ rc=**124**；再核对 CI 那条按码分流的格子会把 124 判成失败而非跳过（`ci.yml` 的 `124|137|143` 分支）。★ 标注：这一格属**门禁契约**，不是产品功能；读一次即可销账 |

### 跨腿共格提示（写在新两节开头，不改写任何历史行）

- W4-1 / W4-2 的"复制 + 校验 + `identityStill` + 删源"记账腿：Linux 真机对格见 **LNX-2**。
- W5-5 / W5-6（大小写敏感目录、根去重折叠）：Linux 真机对格见 **LNX-3**；darwin 对格是 **MAC-3** 的副读数。
- W8-5（中文/emoji/特殊字符全链路）与 W8-1（盘根扫描保护）属**三腿共格**：判据同一条，各腿执行时
  把读数分别回填 W 段对应行的 Darwin/Linux 附注（本批不给它们另立 ID，避免同一判据三处漂移）。

## 5. 明确不收录（已兑现，列出来是为了防止下批复抄）

| 候选 | 为什么不进清单 |
|---|---|
| D-2 的 macOS runner **首跑**（04 §6.8.9 三"macOS runner"那一条） | 已兑现：macos 腿此后反复真跑并抓红过一次（ci.yml 注释 ① 自认，run `35644606017` 的 DATA RACE）；本批推送后又是三腿 `success` |
| XDG 守卫的**行为**面（§31/批 A） | `trash_xdg.go` 无 build tag ⇒ darwin/linux/windows 三腿与开发机同判据真跑，04 §6.29 四已给读数。留在清单上的只有"真两卷"那一半 = **LNX-2** |
| M84 的 `sysguard` 清单本身（八条 `/private` 后代） | 判据是纯字符串表，主门禁 12 条用例真跑（`/private/tmp` 与 `/private/var/folders` 必须敞开已被断言）。未兑现的只有"真扫 `/` 的代价" = **MAC-3** |
| M45（`GetCompressedFileSizeW` 无对照钉子）、M46（`App.d.ts` 不在门禁面上） | 一条属 windows 清单已有格（W2-4/W5-8），一条属构建面，都不是 darwin/linux 真机格 |

## 6. 验证与交付

- 纯文档批仍跑**全套 15 行门禁**（工作规矩：收口=提交+推送+盯 CI），读数逐条现取不抄上批。
- 引用自检：新增两节里每一条"在册出处"都要能在 04/09/ci.yml/源码里 grep 到对应短语；本批写完后逐条
  跑一遍 grep 并把命中数记进 §6.37。
- 登记表**追加** M209 一行（历史行一字不改），划账 §6.37。
- ★ 本批交付的是**清单**，不是读数。MAC/LNX 合计**十一格**里只有 LNX-1 可以就地取材——任何一台 Linux
  机器上带 `-v` 跑一次就有一条读数（本仓库 CI 的 ubuntu 腿也算）。但要说清它的边界：那条用例取的是
  **第一个"dev 真不同"的候选**，一台机器只会点到一个名字，04 挂账要的"六个候选逐个取证"仍然需要
  各自形态的机器（容器里 `/tmp` 是独立 tmpfs、极简镜像里没有 `/run`，都是不同的读数来源）。
  其余十格一律要求现场环境（GUI 会话、真实回收站、第二物理卷、iCloud 账户、NFS/FUSE 服务端），
  **CI 绿不改变它们"未兑现"的状态**。

## 7. 当日更正（推送后 CI 读数回来，两条口径被现读改窄）

设计段写完即推送，CI run `35966760555`（head `5fd62c4`）三腿全绿后逐条现读日志，本节上面两处
写法被当日读数改窄。**上面两段的原文一字未删**，效力以本节为准；读数明细在 04 §6.37 五。

1. **§3 那条"第二物理卷上的真 EXDEV"过强**（连带 §0.1 第 3 条、§6 末段的"其余十格……第二物理卷"）。
   现读 ubuntu 腿"跨卷软链接冒烟"当轮原文：`✓ 已挂载 ext4（/dev/loop0）`、`卷 A dev=2049 ｜ 卷 B dev=1792`、
   ① 硬链接跨卷拿到内核报的**真实** `Invalid cross-device link`、`OK: 跨卷软链接的 7 条核心判据全部成立`
   ⇒ CI 在 ubuntu 腿上**给得出**独立卷与真 EXDEV，只是它覆盖的是文件系统语义那七条、
   不是"回收站复制腿（`copy + .trashinfo + 删源`）在真卷上端到端"。
   05 的三处措辞（§0.1 第 3 条、LNX-2 的"为什么 CI 不算"、§0.3-L 的"卷"）已按此收窄，
   并顺带**下调 LNX-2 的环境代价**：不必另找第二块物理盘，同一 loop 卷上把 `XDG_DATA_HOME` 指过去即可。
2. **§6 末段"LNX-1……本仓库 CI 的 ubuntu 腿也算"这一半不成立**。三腿日志里 `跨卷对照成立` **零命中**：
   不是没跑那条用例，是三条腿的 `go test` 都不带 `-v`（04 §6.11 的 **M100**）⇒ 点名"第一个 dev 不同候选"
   的那行被吞。所以 LNX-1 的"就地取材"要在**自己那台机器上带 `-v` 跑**，CI 现有产物读不到它；
   05 §0.2 的 **A1**（windows 空清单分支补 `-v`）落地后，同一格在 CI 上才顺带可销。
