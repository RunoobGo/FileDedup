# ---------- 构建产物（Wails 官方模板约定）----------
# 注意：main.go 的 //go:embed frontend/dist 需要 frontend/dist 存在，
# 因此全新克隆后须先构建前端（npm run build，或 wails build 自动执行）再跑 go build/test。
build/bin
frontend/dist

# 本地 `go build .` 会在仓库根产出与模块同名的可执行文件（filededup），
# 极易被误提交（本次审查过程中就实际生成过一次）。
/filededup
/filededup.exe

# ---------- 前端依赖 ----------
node_modules

# ---------- Wails 生成物（每次 build 重新生成；源码不引用，见 07 报告遗留 2）----------
frontend/wailsjs
frontend/package.json.md5

# ---------- macOS ----------
.DS_Store

# ---------- Windows ----------
# 与上面的 .DS_Store 同一件事：本机浏览/扫描目录时系统自己写下的元数据。
# 本项目在 macOS 与 Windows 11 两侧交替开发（2026-09-26 §6.43），原先只防了 mac 那一侧。
Thumbs.db
desktop.ini

# ---------- 本地 Agent 会话状态 ----------
# .workbuddy/ 存放项目工作日志/记忆，属本机运行状态而非项目源码，不入库。
.workbuddy
